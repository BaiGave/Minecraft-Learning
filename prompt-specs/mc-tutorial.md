然后根据分析和源代码  对应地搞一个团队 给它写一个面向新人的教程 要简单易懂 放入的文件夹位置可以参考D:\Minecraft-Learning\content\mc\1.21 里面文件夹布局的格式 

## 写作要求

1. **源码路径写法**：`D:\Minecraft-Learning\assets\{模组文件夹名}` 下对应文件的相对路径  
2. **风格完全对齐** `content/mc/1.21/tutorials/`：  
   - 开篇说明本章目标与前置知识  
   - 含 `## 目录` 并用 `[标题](#锚点)` 跳转  
   - 关键术语首次出现时简短解释  
   - 使用 `✅`、`💡`、ASCII 示意图等  
   - **至少一个可运行的小例子**（从开发者视角走通一条逻辑）  
   - **至少 1～2 张 Mermaid 图**（路线图 / 流程图，`flowchart TB`，subgraph ID 用字母数字）  
3. **Mermaid 语法要点**（写完在脑中过一遍）：  
   - `subgraph ID["显示标题"]`，ID 不能有空格  
   - 节点文字含中文/emoji 用方括号：`A["文字"]`  
   - 每个 `subgraph` 要有 `end`，每个引号要成对  
   - 复杂图优先拆成两张，降低语法错误率  
4. **代码块**：` ```java ` 单独成行，不用行号前缀  
5. 文末可附 **3～5 条「课后自查」**

---

## 三、Frontmatter

```yaml
---
title: 章节中文标题
readingTime: 30
---
```

---

## 四、与分析文档的区别

| 维度 | 教程（本提示词） | 分析文档 |
|------|------------------|----------|
| 读者 | 新手为主 | 有基础 |
| Mermaid | 必含、尽量少错 | 可选 |
| 代码 | 精选片段 + 讲解 | 可更密、更偏架构 |
| 语气 | 友好、有举例 | 客观 |
