---
description: Mod 分析文档存放规则 - assets 模组分析后保存到 content 文件夹
alwaysApply: true
---

# Mod 源码分析与文档存放规则

## 目录结构（必须遵守）

分析文档**必须**放在版本分支目录下：

```
content/{模组名}/
└── {MC版本}-{加载器}-{模组版本}/      ← 必须使用这种格式
    └── analysis/                        ← 分析文档放在这里
        └── *.md
```

**正确示例（参考 sodium、iris）：**

```
content/sodium/1.21/fabric/0.8.6/analysis/
content/iris/1.21/fabric/1.7.3/analysis/
content/voxy/1.21.11-fabric-0.2.13-alpha/analysis/
```

❌ **错误示例（无版本分支）：**

```
content/voxy/analysis/               ← 缺少版本信息
content/voxy/1.21/analysis/          ← 缺少加载器和模组版本
```

## 版本目录命名规范

| 字段 | 说明 | 示例 |
|------|------|------|
| `{MC版本}` | Minecraft 版本号 | `1.21`、`1.21.11` |
| `{加载器}` | 加载器小写 | `fabric`、`neoforge` |
| `{模组版本}` | 模组版本号 | `0.2.13-alpha`、`6.0.6` |

完整格式：`{MC版本}-{加载器}-{模组版本}`，三部分用**短横线分隔**，无空格。

## 分析文档命名规范

| 文档类型 | 命名格式 | 示例 |
|---------|---------|------|
| 架构分析 | `XX-{子系统名}.md` | `01-architecture-overview.md` |
| 总结索引 | `SUMMARY.md` | `SUMMARY.md` |

- 使用 `XX-` 前缀表示章节序号（01, 02, 03...）
- 使用 **kebab-case**（短横线分隔）命名
- 避免中文文件名，使用英文描述

## 源码路径引用

在文档中引用模组源码时，使用相对路径格式：

```
D:\Minecraft-Learning\assets\{模组文件夹名}\src\...
```

例如：
- 分析 sodium 模组：`D:\Minecraft-Learning\assets\sodium\src\...`
- 分析 iris 模组：`D:\Minecraft-Learning\assets\iris\src\...`

## 分析文档 vs 教程文档

| 维度 | 分析文档 | 教程文档 |
|------|---------|---------|
| 读者对象 | 有基础的开发者 | 新手为主 |
| 目的 | 理解系统架构 | 学会使用/开发 |
| 代码密度 | 可以更密 | 精选片段 + 讲解 |
| 示例 | 可选 | **必须**至少一个可运行示例 |
| 图表 | 可选 | **必须**至少 1-2 张 Mermaid 图 |

## 教程文档格式要求

```yaml
---
title: 章节中文标题
readingTime: 30
---
```

教程需包含：
1. 开篇说明本章目标与前置知识
2. `## 目录` 并用 `[标题](#锚点)` 跳转
3. 关键术语首次出现时简短解释
4. 使用 `✅`、`💡`、ASCII 示意图等
5. **至少一个可运行的小例子**
6. **至少 1-2 张 Mermaid 图**
7. 文末可附 **3-5 条「课后自查」**

## Mermaid 语法要点（必须遵守）

### 尖括号转义（最容易出错）

节点文案中含 Java 泛型（`RegistryKey<T>`）或命令占位符（`<player>`）时，**必须**用实体写法 `#lt;` / `#gt;`：

| 要写的文字 | 正确写法 | 错误写法 |
|-----------|----------|---------|
| `RegistryKey<Block>` | `RegistryKey#lt;Block#gt;` | `RegistryKey<Block>` |
| `<player>` | `#lt;player#gt;` | `<player>` |

### subgraph ID 规范

- `subgraph ID["显示标题"]`，**ID 不能有空格**，只用字母数字和连字符
- 节点文字含中文/emoji 用方括号：`A["文字"]`
- 每个 `subgraph` 要有 `end`，每个引号要成对
- 复杂图优先拆成两张

## 代码块格式

```java
// 使用 ```java 单独成行，不用行号前缀
```

## 表格渲染规范

- **不要把表格放进引用块（`>`）里**，多行表格在引用块中会被解析器错误处理，导致表格乱码。
- 表格单元格内含带下划线的名称（如 `ENTITY_TYPE`、`LOOT_TABLE`）时，**必须**用反引号包裹：` ` ` ` ` ` ` `。

## 代码引用格式（cite existing code）

引用仓库中**已存在的代码**，使用三段式引用格式（不带语言标签）：

```startLine:endLine:filepath
// code content here
```

引用**新写的示例代码**，使用标准 markdown 代码块（带语言标签）：

```java
// 示例代码
```

